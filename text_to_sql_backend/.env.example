# --- Auth ---
JWT_SECRET=
JWT_EXPIRE_MINUTES=1440

# --- Encryption (AES-256) for stored DB passwords / LLM api keys ---
DATA_ENCRYPTION_KEY=

# --- Shared secret with text_to_sql_mcp for per-request DB connection tokens ---
DB_CONNECTION_TOKEN_SECRET=

# --- MCP server location ---
MCP_SERVER_URL=http://localhost:8020/mcp

# --- Server ---
HOST=0.0.0.0
PORT=8010
CORS_ORIGINS=http://localhost:5173

SMTP_PASSWORD=
